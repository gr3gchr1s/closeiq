[project]
name = "closeiq"
version = "0.1.0"
description = "Accounting close and reconciliation portfolio project"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.2,<4",
    "python-dotenv>=1.0,<2",
]
[tool.unittest]
start-directory = "tests"
